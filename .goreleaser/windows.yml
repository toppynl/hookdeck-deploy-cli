version: 2
env:
  - GO111MODULE=on
before:
  hooks:
    - go mod download
project_name: hookdeck-deploy-cli
builds:
  - id: hookdeck-deploy-cli-windows
    ldflags:
      - -s -w -X github.com/toppynl/hookdeck-deploy-cli/cmd.version={{.Version}}
    binary: hookdeck-deploy-cli
    env:
      - CGO_ENABLED=0
    main: ./main.go
    goos:
      - windows
    goarch:
      - amd64
archives:
  - files:
      - none*
release:
  prerelease: auto
  mode: append
changelog:
  disable: true
checksum:
  name_template: "{{ .ProjectName }}-windows-checksums.txt"
snapshot:
  name_template: "{{ .Tag }}-next"
